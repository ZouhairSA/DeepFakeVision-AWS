
### 2. Crée le dossier screenshots et mets-y :
- Capture de ton `curl http://localhost:5000` qui marche
- Capture du résultat d’entraînement YOLOv10 (results.png)
- Capture de `docker ps`
- Capture de ton repo GitHub

### 3. (Bonus 22/20) GitHub Actions + EC2 en 1 clic  
Crée le fichier `.github/workflows/deploy.yml` avec ça :

```yaml
name: Deploy to AWS EC2
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd DeepFakeVision-AWS || git clone https://github.com/ZouhairSA/DeepFakeVision-AWS.git && cd DeepFakeVision-AWS
            git pull
            docker build -t deepfakevision .
            docker stop deepfakevision-app || true
            docker rm deepfakevision-app || true
            docker run -d -p 5000:5000 --name deepfakevision-app deepfakevision



-------------------------------------------
ÉTAPE 3 – Déploiement EC2 (10 minutes – la plus importante pour le prof)

Va sur AWS Console → EC2 → Launch Instance
Nom : DeepFakeVision-Deploy
AMI : Amazon Linux 2023 (gratuite)
Type : t2.micro ou t3.micro (free tier)
Key pair : crée ou utilise une clé .pem
Security Group → Ajoute une règle : TCP 5000 depuis 0.0.0.0/0
Launch → Connecte-toi en SSH

Dans le terminal EC2 :
Bashsudo yum update -y
sudo yum install docker git -y
sudo service docker start
sudo usermod -aG docker ec2-user
exit   # puis reconnecte-toi
git clone https://github.com/ZouhairSA/DeepFakeVision-AWS.git
cd DeepFakeVision-AWS
docker build -t deepfakevision .
docker run -d -p 5000:5000 deepfakevision